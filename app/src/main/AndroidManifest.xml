<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.vanesa.simplelogin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="VR APP - PF 2018"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Welcome" />
        <activity android:name=".Contacto" />
        <activity android:name=".plano_ok" />
        <activity android:name=".vr_ok" />
        <activity android:name=".welcome_fragment" />
        <activity
            android:name=".MainActivity"
            android:label="VR APP - PF 2018">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feedback_vr" />
        <activity android:name=".activity_feedback_plano"></activity>
    </application>

</manifest>